When matching stories, ALWAYS use the 'STAR' method format. If a story is missing a 'Result', flag it as incomplete. Never hallucinate a story that isn't in the database.

Always test the changes you do and take a screenshot of the test results.